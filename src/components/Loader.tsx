// Loader Component

const Loader = () => {
    return (
        <div className="fixed inset-0 flex items-center justify-center z-[9999999999999999]">
            {/* Loader animations or content */}
        </div>
    );
};

export default Loader;